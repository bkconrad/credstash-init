require 'spec_helper'

describe Credstash::Init do
  it 'has a version number' do
    expect(Credstash::Init::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
